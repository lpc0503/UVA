#include<bits/stdc++.h>

using namespace std;

#define PROBLEM ""

int main(){
	
	#ifdef DBG
	freopen("UVA" PROBLEM ".in", "r", stdin);
	freopen("UVA" PROBLEM ".out", "w". stdout);
	#endif

	return 0;
}